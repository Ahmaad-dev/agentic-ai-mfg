"""Memory system (PT4 / AP7).

long_term  — episodic cases built from human reviews (AP7.1: write path).
retrieval  — case-based lookup + memory_support (AP7.2, not built yet).
short_term — session context; already implemented in web_server/orchestration_agent since AP2,
             to be consolidated here in AP7.4 without behaviour change.
"""
